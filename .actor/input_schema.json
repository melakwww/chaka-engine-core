{
    "title": "Chaka Engine Input",
    "type": "object",
    "schemaVersion": 1,
    "properties": {
        "start_cycles": {
            "title": "Starting Cycles",
            "type": "integer",
            "description": "Initial cycle count",
            "default": 469244
        },
        "target_valuation": {
            "title": "Revenue Goal",
            "type": "string",
            "description": "Target valuation (e.g., $10,000)",
            "default": "$10,000"
        }
    }
}