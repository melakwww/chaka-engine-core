{
    "title": "Chaka Engine Input",
    "type": "object",
    "schemaVersion": 1,
    "properties": {
        "firebase_key": {
            "title": "Firebase Service Account JSON",
            "type": "object",
            "description": "Open your key.json, copy all text, and paste it here.",
            "editor": "json",
            "isSecret": true
        }
    },
    "required": ["firebase_key"]
}