{
    "title": "Chaka Engine Configuration",
    "type": "object",
    "schemaVersion": 1,
    "properties": {
        "firebase_key": {
            "title": "Firebase Service Account JSON",
            "type": "object",
            "description": "Paste the contents of your key.json file here. This is required for Firebase access.",
            "editor": "json",
            "isSecret": true
        },
        "start_cycles": {
            "title": "Starting Cycles",
            "type": "integer",
            "description": "The initial number of cycles to verify.",
            "editor": "number",
            "default": 469244
        }
    },
    "required": ["firebase_key"]
}