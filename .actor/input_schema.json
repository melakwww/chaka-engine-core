{
    "title": "Chaka Engine Configuration",
    "type": "object",
    "schemaVersion": 1,
    "properties": {
        "target_valuation": {
            "title": "Target Revenue Goal",
            "type": "string",
            "description": "The valuation goal to reach (e.g., $10,000)",
            "default": "$10,000"
        },
        "start_cycles": {
            "title": "Starting Cycles",
            "type": "integer",
            "description": "The cycle count to begin the verification from",
            "default": 469244
        }
    },
    "required": ["target_valuation"]
}